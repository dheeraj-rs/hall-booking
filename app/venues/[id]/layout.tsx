import { generateStaticParams } from './generateStaticParams/page';

export { generateStaticParams };

export default function VenueLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 